---
title: 'Jenkins'
description: 'Cancel all jobs running in Jenkins'
logo: 'jenkins.png'
---

```groovy
import hudson.model.*
import jenkins.model.Jenkins
def q = Jenkins.instance.queue
q.items.findAll { it.task.name.startsWith('') }.each { q.cancel(it.task) }
```