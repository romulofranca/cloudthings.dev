
---
title: 'VSCode'
description: 'launch.json file to debug golang code'
logo: 'vscode.png'
---

```json
{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Launch",
          "type": "go",
          "request": "launch",
          "mode": "auto",
          "program": "${workspaceFolder}",
          "env": {"Foo":"bar"},
          "args": ["foo bar"],
          "cwd": "${workspaceRoot}"
      }
  ]
}
```